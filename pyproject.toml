[project]
name = "dbagent"
version = "0.1.0"
description = "Database agent with checkpointed exploration on DBBench modification tasks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.9.0",
    "python-dotenv>=1.1.0",
    "pymysql>=1.2.0",
]
