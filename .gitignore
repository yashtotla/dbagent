# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Environments
.venv/
venv/
.env

# Packaging / tooling caches
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# macOS
.DS_Store

# NOTE: runs/ is deliberately NOT ignored. The traces are the experiment's
# evidence — the named recovery cases in the write-up are quoted from them.
# Ignore them only if the volume becomes a problem, and say so if you do.
